[project]
name = "trading-bot"
version = "0.1.0"
description = "Research-first futures strategy evaluation and paper-trading toolkit"
requires-python = ">=3.11"

[project.scripts]
futures-research = "futures_research.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
